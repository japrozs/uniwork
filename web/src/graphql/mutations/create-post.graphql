mutation createPost($body: String!) {
    createPost(body: $body) {
        id
        body
        likes
        likeStatus
        attachments
        creatorId
        createdAt
        updatedAt
    }
}
